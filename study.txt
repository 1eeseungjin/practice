ol태그와 ul태그의 차이? (순서)

목표 노드제이에스로 웹앱플리케이션 만들기
자바스크립트 문법 - 노드제이에스 기능 이용 가능 - 앱플리케이션

웹 브라우저(주소입력, 요청) - 웹 서버(아파치/노드제이에스, 응답)
요청 - 응답 관계

node.js runtime node.js app 을 실행시킴

console.log(1+1); - 화면에 1+1을 출력

터미널에서 나갈 때는 ctrl c, 두 번 누르기
~ 은 사용자 즉 iseungjin
pwd 는 현재 경로 파악하기
ls -al 모든 파일 목록 자세히 보기(ls -a + ls -l)
ls -a 모든 숨겨진 파일 보기
ls -l 파일에 대한 자세한 정보 
ls -h 사람이 보기 편한 파일 사이즈로 자동 단위 변환
ls -s 파일 크기 순 정렬
ls -d* 디렉토리만 반환
ls ../ 부모 디렉토리 표시
ls -R 하위 디렉토리 표시
ls -t 수정된 파일 순으로 정렬 표시
ls -u 최근 엑세스 날짜 조회 -ul (보기편함)

데이터를 어떻게 처리할 것인가?
데이터의 종류와 처리방법이 중요

js는 데이터 타입이

number / string / boolean / array / object 

변수 variable

